<?js
    var data = obj;
    var self = this;
    var temp = null;
    data.forEach(function(name, i) { ?>
<span class="param-type"><?js= self.linkto(name, self.htmlsafe(name)).replace(/Class\.&lt;.+>/g, function (match) {
    return match.substring('Class.&lt;'.length, match.length - '>'.length);
}) ?></span>
<?js if (i < data.length-1) { ?>|<?js } ?>
<?js }); ?>